# PostHog Analytics (optional)
# Get your keys at https://posthog.com
VITE_PUBLIC_POSTHOG_KEY=
VITE_PUBLIC_POSTHOG_HOST=

# Agent bridge (dev only). When enabled, EditorInterface opens a WebSocket
# connection to a local agent process (e.g. a Python harness driving the
# Anthropic SDK). See apps/web/src/bridges/agent-bridge.ts for the protocol.
# VITE_ENABLE_AGENT_BRIDGE=true
# VITE_AGENT_BRIDGE_URL=ws://localhost:8765
